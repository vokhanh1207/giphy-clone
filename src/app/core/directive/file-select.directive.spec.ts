import { FileSelectDirective } from './file-select.directive';

describe('FileSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSelectDirective();
    expect(directive).toBeTruthy();
  });
});
